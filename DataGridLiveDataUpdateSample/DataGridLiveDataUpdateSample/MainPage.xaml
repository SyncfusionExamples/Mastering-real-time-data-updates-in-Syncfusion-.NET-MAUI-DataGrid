<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DataGridLiveDataUpdateSample"
             xmlns:grid="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             x:Class="DataGridLiveDataUpdateSample.MainPage">

    <ContentPage.BindingContext>
        <local:StockViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:TextForegroundConverter x:Key="textForegroundConverter" />
            <local:ImageConverter x:Key="imageConverter" />
        </ResourceDictionary>
        <Style TargetType="grid:DataGridHeaderCell">
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>
    </ContentPage.Resources>


    <grid:SfDataGrid x:Name="dataGrid"                   
                   x:DataType="local:StockViewModel"
                   ItemsSource="{Binding Stocks}"               
                   AutoGenerateColumnsMode="None"
                   HorizontalScrollBarVisibility="Always"
                   VerticalScrollBarVisibility="Always">

        <grid:SfDataGrid.Columns>
            <!-- Symbol Column -->
            <grid:DataGridTextColumn MappingName="Symbol" HeaderText="Symbol" HeaderTextAlignment="Center" CellTextAlignment="Center"/>

            <!-- Stock Column -->
            <grid:DataGridTemplateColumn HeaderText="Stock" MappingName="StockChange">
                <grid:DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Grid HorizontalOptions="Center" VerticalOptions="Center" ColumnSpacing="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Label x:Name="label"
                               Grid.Row="0"
                               Grid.Column="0"
                               FontSize="40"
                               x:DataType="local:Stock"
                               Text="{Binding StockChange,
                               Converter={StaticResource imageConverter}, 
                               ConverterParameter={x:Reference label}}" />

                            <Label x:Name="changeValue"
                               Grid.Row="0"
                               Grid.Column="1"
                               x:DataType="local:Stock"
                               Text="{Binding StockChange,
                               Converter={StaticResource imageConverter}}"
                               VerticalTextAlignment="Center"
                               FontSize="14" />
                        </Grid>
                    </DataTemplate>
                </grid:DataGridTemplateColumn.CellTemplate>
            </grid:DataGridTemplateColumn>

            <!-- Open Column -->
            <grid:DataGridTextColumn CellTextAlignment="Center"
                                   HeaderTextAlignment="Center"
                                   HeaderText="Open"
                                   MappingName="Open">
               
            </grid:DataGridTextColumn>

            <!-- Previous Close Column -->
            <grid:DataGridTextColumn HeaderText="Previous" MappingName="PreviousClose" HeaderTextAlignment="Center" CellTextAlignment="Center">
               
            </grid:DataGridTextColumn>

            <!-- Last Trade Column -->
            <grid:DataGridTemplateColumn HeaderText="Last Trade"
                                       HeaderTextAlignment="Center"
                                       CellTextAlignment="Center"
                                       MappingName="LastTrade">
                
                <grid:DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Label x:Name="lasttradeValue"
                           x:DataType="local:Stock"
                           Text="{Binding LastTrade}"
                           TextColor="{Binding LastTrade,
                                       Converter={StaticResource textForegroundConverter}}"
                           VerticalTextAlignment="Center" HorizontalOptions="Center">
                        </Label>
                    </DataTemplate>
                </grid:DataGridTemplateColumn.CellTemplate>
            </grid:DataGridTemplateColumn>
        </grid:SfDataGrid.Columns>
    </grid:SfDataGrid>

</ContentPage>
